using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FormValidatorFormatter : MonoBehaviour
{
    // Start is called before the first frame update
    private void Start()
    {
    }

    // Update is called once per frame
    private void Update()
    {
    }

    public virtual string FormatTextForValidate(string value)
    {
        // format text

        return value;
    }

    public virtual string FormatTextForSave(string value)
    {
        // format text

        return value;
    }
}